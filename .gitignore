# Secrets — không bao giờ commit
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Streamlit local config có thể chứa secrets
.streamlit/secrets.toml

# Lưu ý: db/aqi_history.db CỐ Ý không bị ignore — file này được commit ngược
# lại vào repo bởi GitHub Actions sau mỗi lần chạy, để giữ lịch sử AQI dù
# runner của Actions không có state cố định giữa các lần chạy.
.DS_Store
